// Vitest stub for the `server-only` package. The real package throws on import
// outside a React Server Component graph; unit tests import server modules
// directly, so it is aliased to this no-op in vitest.config.mts.
export {};
