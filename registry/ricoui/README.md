# RICOUI Video original components

Place new, original RICOUI Video registry components in this directory. Public
installation remains flat (`@ricoui-video/<name>`); this source directory is an
internal maintenance detail and must not appear in consumer install paths.

Register each component in the root registry build and add its provenance to
`registry/metadata/components.ts` if the automatic RICOUI fallback is not
sufficient.  